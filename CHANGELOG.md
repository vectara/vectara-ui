## `main`

**Bug fixes**

- `VuiTooltip` now forwards refs to its child, so wrapping a `VuiPopover` button in a `VuiTooltip` no longer prevents the popover from opening.

**Non-breaking changes**

...

**Breaking changes**

...
